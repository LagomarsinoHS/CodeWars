//Enum

const small = 1;
const medium = 2;
const large = 3;

enum Size {
    Small = 0,
    Medium = 1,
    Large = 2,
    Random = 'ValorQueQuiera'
}

const miSize: Size = Size.Large;
const anothersize = Size.Random;